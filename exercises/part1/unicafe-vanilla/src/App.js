const App = () => {
    const good = 0;
    const neutral = 0;
    const bad = 0;

    const root = document.createElement('div');
    root.textContent = 'code here';
    return root;
};

export default App;
